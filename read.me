🧩 1. Сортировка выбором (Selection Sort)

Определение

Сортировка выбором — это алгоритм, который поочередно выбирает минимальный элемент из неотсортированной части массива и перемещает его в начало.
Постепенно формируется отсортированная часть массива, а неотсортированная уменьшается.

⸻

Объяснение

Алгоритм делит массив на две области:
 • отсортированную (слева);
 • неотсортированную (справа).
На каждом шаге ищется минимальный элемент в неотсортированной части, который затем меняется местами с первым элементом этой области.

⸻

Пример

Исходный массив: [29, 10, 14, 37, 13]
После сортировки: [10, 13, 14, 29, 37]

⸻

Временная сложность
O(n²)


🧩 2. Сортировка обменом (Bubble Sort)

Определение

Сортировка обменом, или пузырьковая сортировка, — это алгоритм, который многократно проходит по массиву, сравнивая соседние элементы и меняя их местами, если они расположены неправильно.
Самые большие элементы “всплывают” к концу массива, как пузыри.

⸻

Объяснение

Алгоритм повторно сравнивает пары соседних элементов и меняет их местами, если левый элемент больше правого.
После каждого прохода самый большой элемент оказывается в конце массива, и количество проверок постепенно сокращается.

⸻

Пример

Исходный массив: [5, 1, 4, 2, 8]
После сортировки: [1, 2, 4, 5, 8]

⸻

Временная сложность
 • Лучший случай: O(n) (если массив уже отсортирован)
 • Средний случай: O(n²)


🧩 3. Сортировка вставками (Insertion Sort)

Определение

Сортировка вставками — это алгоритм, который формирует отсортированный массив постепенно, вставляя каждый новый элемент в правильное место среди уже упорядоченных.
Принцип похож на то, как человек сортирует карты в руке.

⸻

Объяснение

Алгоритм начинается со второго элемента массива.
Он сравнивается с предыдущими, и если оказывается меньше — перемещается влево, пока не встанет на своё место.
Так шаг за шагом формируется отсортированная часть массива.

⸻

Пример

Исходный массив: [12, 11, 13, 5, 6]
После сортировки: [5, 6, 11, 12, 13]

⸻

Временная сложность
 • Лучший случай: O(n)
 • Средний случай: O(n²)
 • Худший случай: O(n²)



🧩 4. Сортировка слиянием (Merge Sort)

Определение

Сортировка слиянием — это алгоритм, основанный на принципе “разделяй и властвуй”.
Он разбивает массив на части, рекурсивно сортирует их, а затем объединяет в один отсортированный массив.

⸻

Объяснение

Массив делится на две части, каждая из которых сортируется отдельно.
Затем отсортированные подмассивы объединяются с помощью функции merge, которая аккуратно сливает их в порядке возрастания.

⸻

Пример

Исходный массив: [38, 27, 43, 3, 9, 82, 10]
После сортировки: [3, 9, 10, 27, 38, 43, 82]

⸻

Временная сложность
O(n log n)


🧩 5. Сортировка Шелла (Shell Sort)

Определение

Сортировка Шелла — это улучшенная версия сортировки вставками.
Вместо сравнения соседних элементов она начинает с элементов, находящихся на определённом расстоянии (gap), постепенно уменьшая этот шаг, пока не дойдёт до обычной сортировки вставками при gap = 1.


⸻

Объяснение

Алгоритм разбивает массив на группы элементов, разделённых шагом gap, и сортирует каждую группу методом вставки.
С каждым проходом шаг уменьшается, что ускоряет “сближение” элементов к своим позициям, пока не произойдёт окончательная сортировка.

⸻

Пример

Исходный массив: [12, 34, 54, 2, 3]
После сортировки: [2, 3, 12, 34, 54]

⸻

Временная сложность
 • Лучший случай: O(n log n)
 • Средний случай: ≈ O(n^1.5)
 • Худший случай: O(n²)


🧩 6. Быстрая сортировка (Quick Sort)

Определение

Быстрая сортировка — один из самых эффективных алгоритмов сортировки.
Работает по принципу “разделяй и властвуй”: выбирает опорный элемент (pivot), делит массив на элементы меньше и больше него, а затем рекурсивно сортирует обе части.

⸻

Объяснение

Массив делится относительно опорного элемента на две части:
— левая содержит элементы меньше pivot,
— правая — больше или равные.
Обе части сортируются рекурсивно, после чего объединяются в итоговый массив.

⸻

Пример

Исходный массив: [10, 7, 8, 9, 1, 5]
После сортировки: [1, 5, 7, 8, 9, 10]

⸻

Временная сложность
 • Средний случай: O(n log n)
 • Худший случай: O(n²)


🧩 7. Пирамидальная сортировка (Heap Sort)

Определение

Пирамидальная сортировка (или Heap Sort) — это алгоритм, основанный на структуре данных “двоичная куча” (heap).
Сначала из массива создаётся max-куча, где каждый родительский элемент больше своих потомков, а затем из неё последовательно извлекаются максимальные элементы.

⸻

Объяснение
 1. Сначала строится max-куча, чтобы на вершине оказался самый большой элемент.
 2. Затем этот элемент перемещается в конец массива.
 3. После каждой итерации размер кучи уменьшается, а её структура восстанавливается.
 4. Повторяем, пока не отсортируем все элементы.

⸻

Пример

Исходный массив: [12, 11, 13, 5, 6, 7]
После сортировки: [5, 6, 7, 11, 12, 13]

⸻

Временная сложность
O(n log n)


🧩 8. Последовательный поиск (Linear Search)

Определение

Последовательный (линейный) поиск — это простой алгоритм, который проходит по всем элементам массива по очереди, пока не найдёт нужное значение или не достигнет конца массива.

⸻

Объяснение

Алгоритм проверяет элементы массива последовательно:
если текущий элемент совпадает с искомым — поиск завершается;
если нет — переходит к следующему, пока не дойдёт до конца.

⸻

Пример

Массив: [4, 2, 7, 1, 9, 5], поиск числа 7 → найден на позиции 2.

⸻

Временная сложность
 • Лучший случай: O(1) — элемент найден сразу.
 • Средний случай: O(n/2) ≈ O(n).
 • Худший случай: O(n) — элемент отсутствует или находится в конце списка.


🧩 9. Бинарный поиск (Binary Search)

Определение

Бинарный поиск — это эффективный алгоритм поиска элемента в отсортированном массиве.
Он многократно делит диапазон поиска пополам, сравнивая искомый элемент со значением в середине диапазона.

⸻

Объяснение

Алгоритм ищет элемент, каждый раз уменьшая диапазон поиска в два раза.
Если искомое значение меньше среднего, поиск продолжается в левой половине, иначе — в правой.
Так поиск завершается за логарифмическое число шагов.

⸻

Пример

Массив: [2, 4, 6, 8, 10, 12, 14], искомый элемент: 10
→ найден на позиции 4.

⸻

Временная сложность
 • Лучший случай: O(1)
 • Средний случай: O(log n)

🧩 10. Интерполяционный поиск (Interpolation Search)

Определение

Интерполяционный поиск — это усовершенствованный вариант бинарного поиска, который оценивает вероятное положение искомого элемента на основе пропорционального распределения значений.
Хорошо работает для равномерно распределённых данных.

⸻

Объяснение

Вместо деления диапазона пополам, как в бинарном поиске,
алгоритм оценивает позицию элемента, исходя из соотношения его значения к диапазону значений массива.
Если данные распределены равномерно, поиск проходит быстрее.

⸻

Пример

Массив: [10, 20, 30, 40, 50, 60, 70, 80], искомый элемент: 60
→ найден на позиции 5.

⸻

Временная сложность
 • Средний случай: O(log log n)
 • Худший случай: O(n)

🧩 11. Поиск Фибоначчи (Fibonacci Search)

⸻

Определение

Поиск Фибоначчи — это алгоритм поиска элемента в отсортированном массиве, который использует числа Фибоначчи для определения границ подмассива, где может находиться искомое значение.
Он схож с бинарным поиском, но использует деление в пропорции чисел Фибоначчи, что делает его особенно удобным для массивов, хранящихся в последовательной памяти (например, на ленте).

⸻

Объяснение
 1. Алгоритм подбирает ближайшее число Фибоначчи, большее или равное длине массива.
 2. С помощью чисел Фибоначчи выбираются позиции, на которых выполняется сравнение.
 3. В зависимости от результата сравнения диапазон поиска уменьшается, используя меньшие числа Фибоначчи.
 4. Процесс продолжается, пока не будет найден элемент или диапазон не сократится до нуля.

⸻

Пример

Массив: [10, 22, 35, 40, 45, 50, 80, 85, 90, 100], искомое значение: 85
→ найден на позиции 7.

⸻

Временная сложность
 • Худший случай: O(log n)
 • Лучший случай: O(1)
