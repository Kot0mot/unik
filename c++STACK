#include <iostream>
#include <stack>
using namespace std;

int main() {
    stack<char> st;

    st.push('a');
    st.push('b');
    st.push('c');

    cout << "Верхушка стека: " << st.top() << endl; // c
    st.pop();
    cout << "Теперь верхушка: " << st.top() << endl; // b

    return 0;
}
